#include<stdio.h>

int main(){
    // operators
    // arathmatic +, - / % * 
    //logical op && || 
    // bitwise &, | , ~, >>, <<, ^
    //ternary op    (condition)? (true):(false);
    //assignment = . +=, -=, /= *= %=
    //increment decrement ++i, i++, i--, --i 
    
    // comparison op <, >, <=, >= !=, ==

    const int a = 10;

    /*
    int 2 or 4
    char 1
    float 4
    double 8
    bool
    */

   /*
   array
   structure
   union
   classes (c++)
   string
   */

}
